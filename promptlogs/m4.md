# Prompt Log – Rights/Auth (Solano, Vinz Eulo A.)

### **Role:** Rights & Authentication Specialist

# Entry 1 ([Prompt Link](https://gemini.google.com/share/e5c2cd876e57))

**Date:** April 1, 2026

**Task:**

- [#9 Automatically assign professor Ezperanza's account as SUPERADMIN](https://github.com/BaiSakinaAbad/HOPE_INC_Customer_Management_System/issues/9)
- [#15 As a new USER, I want to register so that I can access the system](https://github.com/BaiSakinaAbad/HOPE_INC_Customer_Management_System/issues/15)

## **Prompt given to AI:**

- I want to integrate the login/register in this code base. Analyse the code, tell me the step-by-step process of what to do, and explain why you did this process. Keep the code base clean.

## **What the AI produced:**

This is a great, clean UI foundation. To integrate Supabase authentication (email/password and Google OAuth) while keeping your codebase maintainable, we need to separate the authentication logic from the UI components.

Here is the step-by-step process to wire up your UI to Supabase.

#### **Step 1:** Initialize the Supabase Client

- Created `.env.local` and stored environmental variables
- Create a new file `src/lib/supabase.ts`

#### **Step 2:** Create an Auth Context

- Create `src/components/AuthContext.tsx:`
- Wrap your `AuthProvider` in App.tsx

#### **Step 3:** Update RegisterPage.tsx

Update the state and `handleSubmit` in `RegisterPage.tsx`

#### **Step 4:** Update LoginPage.tsx

Update the state and `handleSubmit` in `LoginPage.tsx`

#### **Step 5:** Wire up Google OAuth

Update `GoogleButton` in `components/composites/AuthComposites.tsx`

## **What I changed / improved:**

- installed supabase package
- created lib/supabase.ts for supabase utilisation (backend)
- AuthComposites.tsx: integrated Supabase client to the 'continue with Google' button so that it proceeds to OAuth
- LoginPage.tsx/RegisterPage.tsx: wired input fields, and connected the form submission to Supabase's email and password authentication method.

## **Why:**

- We need a single, shared instance of the Supabase client to interact with the backend. Creating it in a dedicated file prevents redundant network calls and keeps your configuration centralized.
- Authentication state (like whether a user is logged in or not) needs to be accessed globally. An Auth Context prevents "prop drilling" and gives you a clean way to protect routes later.
- We need to capture the form data and pass it to Supabase. Supabase's `signUp` function accepts a `user_metadata` object, which is perfect for storing the extra fields (First Name, Last Name, Username) you have in your UI.
- Similar to registration, we need to bind the inputs to React state and call `supabase.auth.signInWithPassword`.
- To make the `GoogleButton` functional, you just need to call `signInWithOAuth`. Make sure you have configured Google as an Auth Provider inside your Supabase dashboard first.

# Entry 2 ([Prompt Link](https://gemini.google.com/share/e5c2cd876e57))

**Date:** April 6, 2026

**Task:**

- Integrating Supabase Authentication (Email/Password & Google OAuth) into an existing React, TypeScript, and Tailwind codebase.

## **Prompt given to AI:**

- I already have google cloud and supabase set-up. I have these pieces of code already... I want to integrate the login/register in this code base. Analyse the code, tell me the step-by-step process of what to do, and explain why you did this process. Keep the code base clean.

## **What the AI produced:**

- I already have google cloud and supabase set-up. I have these pieces of code already... I want to integrate the login/register in this code base. Analyse the code, tell me the step-by-step process of what to do, and explain why you did this process. Keep the code base clean.

## **What I changed / improved:**

- I iteratively guided the AI to fix strict TypeScript compilation errors regarding type-only imports and unresolved path aliases. Instead of accepting partial code snippets, I requested the complete, fully written files for the login, register, and composite components. Finally, I asked the AI to distill its explanations down to a single sentence per file.

## **Why:**

- To ensure the generated code actually compiled under the project's strict TypeScript configuration without throwing linting errors. Requesting the full files prevented manual copy-paste errors and ensured the UI styling remained perfectly intact alongside the new logic. The one-sentence summaries were necessary to quickly understand the exact purpose of the modifications for future codebase maintainability.

**Next entries will be added as I continue using AI for [tasks].**

# Entry 3 ([Prompt Link](https://gemini.google.com/share/8090d5241d4f))

**Date:** April 11, 2026

**Task:**

- [#15 As a new USER, I want to register so that I can access the system](https://github.com/BaiSakinaAbad/HOPE_INC_Customer_Management_System/issues/15) (REDO)
- [#2 TASK: Create Supabase project, share .env keys, Add db and backend dev to project.](https://github.com/BaiSakinaAbad/HOPE_INC_Customer_Management_System/issues/2)

## **Prompt given to AI:**

- I'm making a login/register page. I already set up the Google authentication sign-in. This time I want the user to be able to sign in/register without Google Auth, by manually typing in their info in the input fields. I also have test cases that will ensure the right output (npm run test).

`<INSERT CODEBASE>`

## **What the AI produced:**

Your authentication logic for email and password using Supabase is actually already completely wired up in your LoginPage and RegisterPage components!

To pass your vitest suite, you just need to add the missing data-testid attributes to your UI components so the tests can find them, and implement the lifecycle guard inside AuthLayout to check the user's record_status.

Here are the specific updates you need to make to your files to satisfy all three test suites:

1. Update AuthElements.tsx
   Update your AuthButton to accept the data-testid prop so it can pass it down to the standard HTML button.

2. Update AuthComposites.tsx
   Add a testId prop to the GoogleButton interface and component.

3. Update LoginPage.tsx
   Pass the required data-testid identifiers into your form elements and the Google button.

4. Update RegisterPage.tsx
   Just add the test ID to the Google button inside this file.

5. Update AuthLayout.tsx
   To satisfy LoginGuard.test.tsx, your Auth shell needs to actively query Supabase on mount and kick the user out if their record_status metadata is marked as INACTIVE

## **What I changed / improved:**

- I didn't change anything apart from the bugs the code AI produced had.

## **Why:**

- To fully setup the user registry and authentication of the website.

# Entry 4 ([Prompt Link]())

**Date:** April 15 & 16, 2026

**Task:**
- Reconstructing Folder structure
- [#64 User and Admin dashboard pages placeholder as assigned by M2](https://github.com/BaiSakinaAbad/HOPE_INC_Customer_Management_System/issues/64)

## **Prompt given to AI:**
- Be upfront and blunt, and don't put my feelings into consideration, be objective. What do you think of our project structure? We're using React, Typescript, Vite, and Supabase. Do you think we should restructure? If so, what do you think we should change? Note that we're making a Customer Management Service
- Guide me, provide me with detailed step-by-step instructions
- In SuperAdminLayout.tsx and Topbar.tsx I added {role}. With the role variable, I want to pull it from supabase table.
- how to test if I'm grabbing from the table correctly

## **What the AI produced:**
- A blunt review explaining that our project folders were too cluttered and messy.

- A step-by-step plan to organize our code by grouping related features together (for example, keeping all login-related code in one folder).

- Instructions on how to clean up our visual styling and fix broken file links.

- The exact database fix for a "loop" error that was preventing the app from checking user roles during login.

## **What I changed / improved:**
- Instead of using the AI's automated commands, I moved, changed, and deleted the files and folders manually.

## **Why:**
- I kept the testing files where they were so I wouldn't confuse the rest of the team who are used to the old setup.
- Fixing the database error was completely necessary. Without fixing that login loop, we wouldn't  be able to fetch from our database

# Entry 5 ([Prompt Link](https://gemini.google.com/share/d6284b9e262b))

**Date:** April 24, 2026

**Task:**
- [#48 Customer List Page – Create the main table with Search/Filter functionality (by name).](https://github.com/BaiSakinaAbad/HOPE_INC_Customer_Management_System/issues/48)
- [#31 As a USER, I want to view all active customers so that I can quickly access contact information and billing details for ongoing business accounts.](https://github.com/BaiSakinaAbad/HOPE_INC_Customer_Management_System/issues/31)
- [#43 As an ADMIN, I want to activate new users so that recently hired employees can begin their work within the system.](https://github.com/BaiSakinaAbad/HOPE_INC_Customer_Management_System/issues/43)
- [#46 As a SUPERADMIN, I want to deactivate users so that I can immediately revoke system access for employees who have left the company or violated security policies.](https://github.com/BaiSakinaAbad/HOPE_INC_Customer_Management_System/issues/46)

## **Prompt given to AI:**
- Review the React codebase and suggest layout and performance improvements.
- Help decide on the best folder structure.
- Refactor `SuperAdminLayout` into a general wrapper.
- Fix bugs with broken routing and missing user display names.
- Split large customer tables into smaller components.
- Create a new page to manage employees and add it to the sidebar.
- Fix the Sign-In button not triggering the login.
- Fix the app getting stuck on the loading screen.

## **What the AI produced:**
- Recommended a single `DashboardLayout` instead of role-specific layouts.
- Explained folder setups and adapted to a flat `src/` structure.
- Rewrote the layout and routing code, and provided bug fixes.
- Created isolated components for search bars, modals, and table rows.
- Built the Employee page with backend connections and role-restrictions.
- Added `type="submit"` to the login button.
- Updated the loading screen to close instantly when user data is ready.

## **What I changed / improved:**
- Flattened the folder structure.
- Separated the visual page layout from the actual page routing.
- Broke massive, slow page files into smaller, reusable pieces.
- Added an employee management page.
- Fixed critical login, loading, and navigation bugs.

## **Why:**
- To make the code cleaner and easier for a team to work on.
- To make the app run faster by preventing full-page reloads on every keystroke.
- To secure specific features so only admins can manage staff and deleted records.
- To ensure users can successfully log in and navigate without getting stuck.

# Entry 6 

**Date:** April 26, 2026

**Task:** User Activation and Login Guard

## **Prompt given to AI:**
My task is to add activation to both users and customers and a login guard. With customers, they can be activated and deactivated by a superadmin. If they are users, they can be blocked, activated, and deactivated. Now, if the user's status is inactive, and they're signed in, they get shown a pop up that they can not close saying that their account is inactive and they should contact the admins. If they're signed out and trying to sign in, they get shown the popup upon signing in. If they're blocked, then they get signed out of the application and displayed a message that their blocked

## **What the AI produced:**
The AI Produced code additions implimenting proper user activation and login guards.

## **What I changed / improved:**
None

## **Why:**
- To ensure that security is working and able to do what it's made to do
- User Activation enables authorization from the admins

# Entry 7 ([Prompt Link]())

**Date:** April 27, 2026

**Task:** Migrating Employee Service to Users

## **Prompt given to AI:**
- change the employees into app_user. I already have the proper table setup. I just need you to implement them into the code. Tell me where to change and what to change. (Note: employees table doesn't have a status)

## **What the AI produced:**
- Step-by-step instructions to migrate from `employees` to `app_user` in components, services, and hooks.
- Instructions to fix the `TypeError: Cannot read properties of undefined (reading 'status')` by removing references to `status` inside the data mapping.
- Updated `useRights` hook, `AuthProvider`, `employeeService`, and `EmployeeListPage` to match the new schema.
- Added a workaround for empty table crashes due to RLS policies.

## **What I changed / improved:**
- None. (I just followed the AI's bug fixes)

## **Why:**
- To align the frontend code with the backend database schema changes (migrating to `app_user`).
- To fix critical page crashes caused by RLS restrictions and empty data arrays.

# Entry 8 ([Prompt Link]())

**Date:** April 28, 2026

**Task:** Enhancing User Security Permissions

## **Prompt given to AI:**
- In Employees. The superadmins should not be able to be deactivated.
- In Employee List Page. The 3 dots action menu, it should have a 'View Permissions' when you click it, it just shows a modal that has the user's permissions, basically just checking whether the user has access to it or not. The header should be "Permissions (UI no functionality)"

## **What the AI produced:**
- Added a safeguard in `EmployeeRow.tsx` to prevent the deactivation button from appearing for superadmin users.
- Created a new modal component `PermissionsModal` inside `EmployeeListPage.tsx` to display user permissions as a read-only list.
- Wired the "View Permissions" option in the 3-dots menu to trigger the modal.
- Designed the modal to include a clear header and a toggle-style visual indicator for access/no-access.

## **What I changed / improved:**
- None.

## **Why:**
- To prevent accidental lockouts of superadmin accounts.
- To provide a quick UI way for admins to audit what permissions a specific employee has.

# Entry 9 ([Prompt Link]())

**Date:** April 29, 2026

**Task:** Finalizing Audit Logs Dashboard

## **Prompt given to AI:**
- the logs record ID should be the id of the logs themselves, not the record ID
- if it's deactivated, it says 'Activated' but if I click it, the data before is inactive and data after is active. It's supposed to be deactivated
- [Asked to fix "Deleted" action detection and add "Restored" action detection]

## **What the AI produced:**
- Fixed the action detection logic in `formatAuditAction` to properly classify events based on `record_status` changes (`ACTIVE` vs `INACTIVE`).
- Added support for "Restored" (when a deleted item is brought back) and "Deactivated" / "Activated" events.
- Updated the table rendering to use the audit log's UUID as the Record ID instead of the target entity's ID.
- Improved the status badge UI to reflect the correct action colors (e.g., Red for Deactivated/Deleted, Green for Activated/Restored).

## **What I changed / improved:**
- None.

## **Why:**
- To make the audit log accurate and readable for administrators.
- To ensure that soft-deletes and status changes are correctly interpreted and displayed in the system history.

# Entry 10 ([Prompt Link]())

**Date:** May 3, 2026

**Task:** Implementing Audit Log Pagination

## **Prompt given to AI:**
- Add a pagination to the audit logs. I changed the folder name of logs/ to audits/ as logs get ignored by .gitignore

## **What the AI produced:**
- Refactored `LogsPage.tsx` to use the `DefaultTable` components to automatically gain the built-in pagination footer.
- Implemented client-side pagination state (15 items per page) and memoized the data slicing.
- Replaced the raw HTML `<table>` with the styled components to match the rest of the app.
- Added a search result count inside the search bar.
- Updated the fetch limit from 200 to 500 in `auditLogService.ts`.

## **What I changed / improved:**
- None.

## **Why:**
- To improve the page load performance and UI consistency, making it easier to browse a large history of audit logs.

# Entry 11 ([Prompt Link]())

**Date:** May 4, 2026

**Task:** Fixing Permissions and Pagination

## **Prompt given to AI:**
- Fix the permissions modal so it displays permission rows even when RLS policies restrict direct table access
- Fix permission toggles not saving to the database
- Fix admin account not being able to see the Users page despite having the ADM_VIEW permission
- Separate the activate and deactivate permission checks (ADM_USER vs ADM_DEACTIVATE)
- Add audit logging for permission changes
- Add a "Reset to Defaults" button in the permissions modal
- Hide "Deleted Customers" sidebar item when CUST_VIEW_INACTIVE permission is false
- Fix broken avatar images with a fallback

## **What the AI produced:**
- A merge-based permission loading strategy in AuthProvider that starts with role defaults and overlays DB results, handling partial RLS reads gracefully
- A `fetchInFlight` ref guard to prevent race conditions from Supabase's dual auth event firing
- An `updateUserPermission` function with `.select()` verification to detect silent RLS write blocks
- A `handleResetPermissions` handler and footer button in the permissions modal
- Audit trigger SQL for `user_permission` table and frontend support for GRANTED/REVOKED event types
- Separated `canActivateUsers` (ADM_USER) and `canDeactivateUsers` (ADM_DEACTIVATE) permission checks
- Avatar `onError` fallback to DiceBear initials
- RLS policy cleanup SQL scripts

## **What I changed / improved:**
- None.

## **Why:**
- To ensure the permission system is fully functional end-to-end — reading, writing, and auditing.
- To fix critical bugs where admins couldn't access pages they had permissions for.
- To provide superadmins with a quick way to reset customized permissions back to defaults.
- To ensure the sidebar accurately reflects the user's actual permissions rather than just their role.

# Entry 12 ([Prompt Link]())

**Date:** May 7, 2026

**Task:** Hardening Role-Based Access Control

## **Prompt given to AI:**
- Alright, there's a problem with the user rights. Admin and User shouldn't be promoted into superadmin.
- Another thing. When the user's role changes, no matter where it got changed, through website or the database, when it changes the default permissions of that role should be given to that user.
- with the dashboard, if you look at the photo, the refresh button and the welcome should be on the same line.
- The Accounts pending Activation should be on the right side as to make up space for the other parts. It should follow when the user is scrolling.
- Customer Sales Summary is good, but you should add a pagination mechanic.
- The total revenue and registered customers should be in one container...
- Make the bar a bit thicker to cover the empty space below it.

## **What the AI produced:**
- Removed "superadmin" from the role dropdown in `EmployeeRow.tsx` and added backend blocks in `employeeService.ts` to prevent promotion to superadmin.
- Added a Supabase Realtime subscription to `AuthProvider.tsx` to detect role changes on `app_user` dynamically.
- Created a SQL trigger (`docs/role_change_trigger.sql`) to auto-reset permissions when a role changes directly in the database.
- Completely redesigned the Dashboard layout (`DashboardReports.tsx`), placing the stat cards in a responsive CSS grid of uniform height.
- Repositioned the "Pending Activation" section into a standalone modal overlay triggered by a button in the header.
- Added pagination to the Customer Sales Summary table.

## **What I changed / improved:**
- None.

## **Why:**
- To harden security by preventing unauthorized escalation of privileges.
- To ensure permissions stay perfectly synchronized with roles, even if changed outside the app.
- To improve the visual balance, space utilization, and aesthetics of the Dashboard.

# Entry 13 ([Prompt Link]())

**Date:** May 10, 2026

**Task:** Enforcing Backend User Rights

## **Prompt given to AI:**
- check user rights as it only reflects on the ui, not the functionality
- How do I test it out? When you made the changes, the website got a bit laggy. Also, when the user can view the dashboard, the user should be directed to that rather than the customer page.
- I am a superadmin and I'm still being redirected to the customer page by default. The permissions are still only reflected through the UI. If there's anything that should be done in the database (SQL) tell me
- Alright, when signing out. It shouldn't store the last page I visited.
- And another bug, when the user is an admin, when they try to change the permission of a user, it only shows read only.
- Another bug, if the user can deactivate a person, for some reason they deactive themselves as well.
- Fill this out: What changed, How to test

## **What the AI produced:**
- Implemented backend enforcement of user rights in service files (e.g., `customerService.ts`, `employeeService.ts`) for all write operations (ADD, EDIT, DEL, RECOVER).
- Added page-level guards via `AccessDenied.tsx` within `Dashboard.tsx` routing.
- Fixed performance lag by removing `permissions` from read-only `useCallback` dependency arrays to prevent double-fetching.
- Fixed default redirect by making `Dashboard.tsx` wait for the role to load and using `canViewDashboard` instead of a hardcoded string.
- Fixed the issue with Admin editing permissions by correctly scoping the `canEditTargetPermissions` variable per permission (Admins can edit user permissions but ADM_* categories are read-only).
- Blocked self-deactivation by adding a current user check.
- Provided SQL statements to fix RLS policies that were blocking `user_permission` database reads.

## **What I changed / improved:**
- Installed `react-router-dom` to resolve missing dependencies after branch merge, and executed SQL provided to fix RLS.

## **Why:**
- To strictly secure the system at the backend level, preventing API abuse if the UI is bypassed.
- To resolve logic bugs with permissions and redirects for a smooth administrative experience.
- To ensure RLS properly permits dynamic permission evaluation.
